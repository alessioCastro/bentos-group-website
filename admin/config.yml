backend:
  name: git-gateway
  branch: main

media_folder: "assets/img/uploads"
public_folder: "/assets/img/uploads"

collections:
  - name: "content"
    label: "Site Management"
    files:

      # ==========================================
      # 1. GALLERY (Groups + Standalone Photos)
      # ==========================================
      - file: "content/gallery.json"
        label: "Photo Gallery"
        name: "gallery"
        fields:
          # --- GROUPS / CATEGORIES ---
          - label: "Photo Groups (Categories)"
            name: "groups"
            widget: "list"
            summary: "Group: {{fields.group_name}}"
            fields:
              - { label: "Group Name", name: "group_name", widget: "string", hint: "E.g., Kitchen, Bathroom, Flooring, Outdoor" }
              - label: "Group Photos"
                name: "photos"
                widget: "list"
                summary: "{{fields.alt}}"
                fields:
                  - { label: "Upload Image", name: "image", widget: "image", required: false }
                  - { label: "OR Image URL", name: "image_url", widget: "string", required: false }
                  - { label: "Alt Text (SEO)", name: "alt", widget: "string", hint: "Description for accessibility and SEO" }

          # --- STANDALONE / UNGROUPED PHOTOS ---
          - label: "General Photos (Ungrouped)"
            name: "standalone_photos"
            widget: "list"
            summary: "{{fields.alt}}"
            hint: "Photos that do not belong to any specific group"
            fields:
              - { label: "Upload Image", name: "image", widget: "image", required: false }
              - { label: "OR Image URL", name: "image_url", widget: "string", required: false }
              - { label: "Alt Text (SEO)", name: "alt", widget: "string" }

      # ==========================================
      # 2. CABINETS CATALOG
      # ==========================================
      - file: "content/cabinets.json"
        label: "Cabinets Catalog"
        name: "cabinets"
        fields:
          - label: "Cabinets List"
            name: "cabinets"
            widget: "list"
            summary: "[{{fields.id}}] {{fields.name}} - ${{fields.pricePerFoot}}/ft"
            fields:
              - { label: "Cabinet ID", name: "id", widget: "string", required: false, hint: "E.g., sjg (Leave blank to generate automatically)" }
              - { label: "Cabinet Name", name: "name", widget: "string", hint: "E.g., Shaker Jade Green" }
              - { label: "Series", name: "series", widget: "string", hint: "E.g., Shaker Series" }
              - { label: "Price Per Foot ($)", name: "pricePerFoot", widget: "number", value_type: "int", hint: "E.g., 225" }
              - { label: "Upload Image", name: "image", widget: "image", required: false }
              - { label: "OR Image URL", name: "image_url", widget: "string", required: false }
              - { label: "Alt Text (SEO)", name: "alt", widget: "string" }